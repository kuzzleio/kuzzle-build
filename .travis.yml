dist: trusty
sudo: required

services:
- docker

before_script:	
- sudo sysctl -w vm.max_map_count=262144

script:
- bash test/run.sh

deploy:
  provider: pages
  skip-cleanup: true
  local-dir: setupsh-badges
  github-token: $GITHUB_TOKEN 
  keep-history: true
  on:
    branch: gh-pages